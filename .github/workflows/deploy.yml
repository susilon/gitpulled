name: Deploy on Push

on:
  push:
    branches: [main]

jobs:
  trigger-deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Trigger AutoDeploy Webhook
        run: |
          curl -X POST "${{ secrets.WEBHOOK_URL }}"
