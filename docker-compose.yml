version: '3.8'

services:
  autodeploy:
    build: .
    container_name: autodeploy
    ports:
      - "3000:3000"
    volumes:
      - ./data:/app/data
    environment:
      - PORT=3000
      - SESSION_SECRET=your-secret-key-change-in-production
    restart: unless-stopped
